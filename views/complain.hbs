


{{> SideBar}}

	<!-- NAVBAR -->
	<section id="content">
		<!-- NAVBAR -->
{{> Nav}}
		<!-- NAVBAR -->

		
		<!-- MAIN -->
		<main>
	
		{{> notsubmain}}

       <section class="container">
    {{#each list}}
    <div class="row py-3 w-100">
  <div class="col">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">{{this.fullname}}</h5>
        <div>
        <h5 class="card-title">{{this.matriculation}}</h5>
        </div>
        <p class="card-text">{{this.compfield}}</p>
       {{> EDITDELETE}}
          
      </div>
    </div>
  </div>
{{/each}}
</section>

	

		</main>
		<!-- MAIN -->
	</section>
	<!-- NAVBAR -->
